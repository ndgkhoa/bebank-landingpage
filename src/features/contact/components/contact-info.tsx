import { ContactTable } from '~/features/home/components/contact';
import Container from '~/components/ui/container';

const ContactInfo = () => {
  return (
    <Container contentName="CONTACT INFO">
      <ContactTable />
    </Container>
  );
};

export default ContactInfo;
